# ============================================================
# ice-frontend 本地开发环境
# 使用: docker compose -f docker-compose.base.yml -f docker-compose.dev.yml up -d
# ============================================================

services:
  frontend:
    image: nginx:alpine
    ports:
      - "3080:80"
    volumes:
      - ./dist:/usr/share/nginx/html:ro
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
